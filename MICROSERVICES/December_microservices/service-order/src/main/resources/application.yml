spring:
  application:
    name: Microservices order

management:
  endpoints:
    web:
      base-path: '/services-order/actuator'
      exposure:
        include: '*'
  health:
    show-details: always
    probes:
      enabled: true
    livenessState:
      enabled: true
    readinessState:
      enabled: true

server:
  port: 8081